<%= form_for(@mealtime) do |f| %>
  <% if @mealtime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mealtime.errors.count, "error") %> prohibited this mealtime from being saved:</h2>

      <ul>
      <% @mealtime.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
