<%= form_for(@course_photo) do |f| %>
  <% if @course_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_photo.errors.count, "error") %> prohibited this course_photo from being saved:</h2>

      <ul>
      <% @course_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :photo_id %><br>
    <%= f.text_field :photo_id %>
  </div>
  <div class="field">
    <%= f.label :course_id %><br>
    <%= f.text_field :course_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
